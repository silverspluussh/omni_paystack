import 'package:flutter_test/flutter_test.dart';

import 'package:omni_paystack/omni_paystack.dart';

void main() {
  test('adds one to input values', () {
    final calculator = OmniPaystack();
    //expect(calculator.onInit(context: , secretKey: secretKey, reference: reference, recipientEmail: recipientEmail, onCompleted: onCompleted, onFailed: onFailed, callBackUrl: callBackUrl, currency: currency, channel: channel, amount: amount), 3);
  });
}
